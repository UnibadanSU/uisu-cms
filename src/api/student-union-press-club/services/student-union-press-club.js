'use strict';

/**
 * student-union-press-club service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::student-union-press-club.student-union-press-club');

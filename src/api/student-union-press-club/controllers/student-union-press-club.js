'use strict';

/**
 *  student-union-press-club controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::student-union-press-club.student-union-press-club');
